{
  "name": "freddys-game",
  "version": "1.0.0",
  "description": "Advanced browser-based 3D ninja castle game with AI characters",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@vitest/coverage-v8": "^2.1.0",
    "happy-dom": "^15.0.0",
    "vite": "^5.0.0",
    "vitest": "^2.1.0"
  }
}
